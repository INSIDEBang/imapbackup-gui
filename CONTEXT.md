# imapbackup-gui

rcarmo/imapbackup 的 fork：在保留命令行工具的同时，为非技术同事提供图形界面与 Windows 安装包。

## Language

**邮箱文件夹 (Folder)**:
IMAP 服务器上的邮件文件夹（如 `INBOX`、`Archive/2024`）。
_Avoid_: 邮箱、目录（"目录"特指磁盘上的文件夹）

**文件夹树 (Folder Tree)**:
邮箱文件夹按服务器路径层级组织成的结构；同一个文件夹节点既可以包含邮件，也可以包含子文件夹。
_Avoid_: 嵌套列表、树形菜单

**文件夹显示名 (Display Name)**:
邮箱文件夹名经 IMAP modified UTF-7 解码后、可直接阅读的形式；未解码的原始形态是服务器返回的 IMAP 名（`&UXZO1mWHTvZZOQ-` 对应 `其他文件夹`）。层级分隔符由服务器选择（`/`、`.` 或 `\`），因此判断一个文件夹是否位于某父文件夹之下要按分隔符切段比较，不能按字符串前缀。
_Avoid_: UTF-7 名、编码名

**文件夹映射**:
把服务器上的邮箱文件夹名称转换为磁盘输出路径的规则；mbox 与 eml 各有一套。
_Avoid_: 路径转换、命名规则

**mbox 输出**:
把一个邮箱文件夹的所有消息追加进单个 mbox 文件的输出格式（上游形态）。
_Avoid_: 邮箱文件、mbox 目录

**eml 输出**:
把每封消息保存为独立 `.eml` 文件、按文件夹嵌套并按月分组的输出格式。
_Avoid_: 单文件导出、逐封导出

**账号目录**:
备份根目录下以邮箱账号命名的一层目录，同一根目录可并存多个账号的备份而不互混。
_Avoid_: 用户目录、账户文件夹

**月度分组 (Bucket)**:
eml 输出中按消息接收时间划分的 `yyyy-MM` 子目录层。
_Avoid_: 日期文件夹、时间分桶

**消息标识 (Message-Id)**:
邮件头中全局唯一的消息标识，是增量备份的判重键。
_Avoid_: UID、序号（IMAP 序号/UID 不用于判重）

**合成消息标识 (Synthetic Message-Id)**:
消息缺失 Message-Id 头时，由固定盐值加头部内容哈希生成的替代标识。
_Avoid_: 假 ID、UUID（UUID 只是盐值常量）

**增量备份**:
只下载本地输出中尚不存在（按消息标识判重）的消息的备份方式。同时启用多种输出格式时，按格式独立判重：任一种已启用格式缺这封就算"尚不存在"，该消息只下载一次、只写入还缺它的格式（见 ADR 0004）。
_Avoid_: 全量备份、同步

**备份运行 (Run)**:
用户发起一次备份到其自然结束（完成、被停止或失败）的完整过程。
_Avoid_: 同步任务、会话

**待备份消息 (Pending)**:
服务器已有而本地输出尚无、本次备份运行将下载的消息；多种输出格式启用时，只要某一种已启用格式缺这封就算待备份。
_Avoid_: 新增、增量部分

**跳过消息 (Skipped)**:
本地输出已存在（按消息标识判重）、本次备份运行不再下载的消息；多种输出格式启用时，只有每一种已启用格式都有它才算跳过。
_Avoid_: 忽略消息、重复消息

**文件夹快照 (Folder Snapshot)**:
从服务器取得的一次邮箱文件夹列表，与取得它时的那组连接参数绑定；连接参数改变即过期，须重新取得。
_Avoid_: 文件夹缓存、目录树

**合成父节点 (Synthetic Parent)**:
只由子文件夹的路径前缀推导出来的中间层，服务器并未返回它；它不指向任何可备份的邮箱文件夹。
_Avoid_: 虚拟文件夹、幻影节点

**只读备份**:
对 IMAP 服务器只执行读取操作的约定（read-only SELECT、BODY.PEEK），绝不把消息标成已读。
_Avoid_: 拉取、抓取（不表达是否 PEEK）
